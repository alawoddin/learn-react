import styled from './Footer.module.css';

const Footer = () => {
    return (
        <div>
            <h1 className={styled.footer}>
                this is for footer
            </h1>
        </div>
    )
}

export default Footer;